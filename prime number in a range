start =int(input("Enter the start number\n "))
last =int (input("Enter the last number \n"))
print("Prime number in "+ str(start) +" and "+ str(last) +" range\n" )

for num in range (start, last+1):
    if(num>1):#1 is not prime number 
        for i in range(2,num):#2 is all time prime number
            if(num%i==0):#check num last all number will devide or not 
                break
        else:
            print("Prime number is :"+str(num))
